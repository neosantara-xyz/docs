Add this configuration to your Claude Desktop configuration file:

```json claude_desktop_config.json icon="file-code"
{
  "mcpServers": {
    "neosantara": {
      "url": "https://api.neosantara.xyz/v1/mcp",
      "headers": {
        "Authorization": "Bearer nsk_mcp_your_key_here"
      }
    }
  }
}
```
