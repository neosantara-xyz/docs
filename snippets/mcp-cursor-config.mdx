Add this configuration to `~/.cursor/mcp.json`:

```json ~/.cursor/mcp.json icon="file-code"
{
  "mcpServers": {
    "neosantara": {
      "url": "https://api.neosantara.xyz/v1/mcp",
      "headers": {
        "Authorization": "Bearer nsk_mcp_your_key_here"
      }
    }
  }
}
```
